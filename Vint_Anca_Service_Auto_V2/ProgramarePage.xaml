<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Vint_Anca_Service_Auto_V2.ProgramarePage"
             Title="ProgramarePage">
    <StackLayout Margin="20">
        <Editor Placeholder="Numar inmatriculare"
             Text="{Binding NrInmatriculare}"    
             HeightRequest="100" />
        <Editor Placeholder="Nume si prenume client"
            Text="{Binding Client}"    
            HeightRequest="100" />
        <Editor Placeholder="Data si ora programarii"
            Text="{Binding DataOra}"    
            HeightRequest="100" />
        <Editor Placeholder="Nume Mecanic"
            Text="{Binding Mecanic}"    
            HeightRequest="100" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Text="Save"
                Clicked="OnSaveButtonClicked" />
            <Button Grid.Column="1"
                Text="Delete"
                Clicked="OnDeleteButtonClicked"/>
        </Grid>
    </StackLayout>
</ContentPage>